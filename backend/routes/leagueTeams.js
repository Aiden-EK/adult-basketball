const express=require('express');const pool=require('../db');const router=express.Router({mergeParams:true});
router.get('/',async(req,res)=>{const leagueId=Number(req.params.leagueId);if(!Number.isInteger(leagueId)||leagueId<1)return res.status(400).json({message:'Invalid league id'});try{if(!(await pool.query('SELECT 1 FROM league WHERE id=$1',[leagueId])).rowCount)return res.status(404).json({message:'League not found'});const r=await pool.query(`SELECT t.id,t.name,t.captain_member_id AS "captainMemberId", captain.name AS "captainName",COALESCE(json_agg(json_build_object('leagueMemberId',lm.id,'memberId',m.id,'name',m.name,'memberType',m.grade) ORDER BY m.name) FILTER(WHERE m.id IS NOT NULL),'[]') members FROM team t LEFT JOIN league_member lm ON lm.team_id=t.id LEFT JOIN member m ON m.id=lm.member_id AND m.is_active=TRUE LEFT JOIN league_member captain_lm ON captain_lm.id=t.captain_member_id LEFT JOIN member captain ON captain.id=captain_lm.member_id WHERE t.league_id=$1 GROUP BY t.id,captain.name ORDER BY t.sort_order,t.id`,[leagueId]);res.json(r.rows);}catch(e){res.status(500).json({message:'서버 오류가 발생했습니다.'});}});module.exports=router;
