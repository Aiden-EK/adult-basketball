import { useEffect,useState } from 'react'
import { useParams } from 'react-router-dom'
import PageTitle from '../../components/PageTitle'
import { ErrorMessage,EmptyState,Loading } from '../../components/Status'
import { getLeague } from '../../services/leagueApi'
import { getLeagueParticipants } from '../../services/participantApi'
import { getLeagueTeams } from '../../services/teamApi'
const tabs=[['standings','순위','아직 순위 정보가 없습니다.'],['games','경기','아직 등록된 경기 정보가 없습니다.'],['participants','참가자','아직 참가자 정보가 없습니다.'],['winner','우승팀','아직 우승팀 정보가 없습니다.']]
export default function LeagueDetailPage(){const{id}=useParams(),[league,setLeague]=useState(null),[participants,setParticipants]=useState(null),[teams,setTeams]=useState([]),[tab,setTab]=useState('standings'),[error,setError]=useState('');useEffect(()=>{getLeague(id).then(setLeague).catch(()=>setError('정보를 불러오지 못했습니다.'))},[id]);useEffect(()=>{if(tab==='participants')Promise.all([getLeagueParticipants(id),getLeagueTeams(id)]).then(([p,t])=>{setParticipants(p);setTeams(t)}).catch(()=>setError('참가자 정보를 불러오지 못했습니다.'))},[id,tab]);const grouped=teams.map(t=>({...t,members:participants?.filter(p=>Number(p.teamId)===Number(t.id))||[]}));const unassigned=participants?.filter(p=>!p.teamId)||[];return <><PageTitle eyebrow="LEAGUE DETAIL" title={league?.name||'리그 상세'} back/>{error?<ErrorMessage text={error}/>:!league?<Loading/>:<><div className="details card"><div className="row"><span>연도</span><b>{league.year}년</b></div><div className="row"><span>분기</span><b>{league.quarter}분기</b></div><div className="row"><span>상태</span><b>{league.status}</b></div></div><div className="tabs">{tabs.map(([key,label])=><button className={tab===key?'selected':''} key={key} onClick={()=>setTab(key)}>{label}</button>)}</div>{tab==='participants'?(participants===null?<Loading/>:participants.length===0?<EmptyState text="등록된 참가자가 없습니다."/>:<div className="team-groups">{grouped.map(t=><section className="card team-group" key={t.id}><h3>{t.name}</h3>{t.members.length?t.members.map(m=><p key={m.memberId}><b>{m.name}</b><span>{m.memberType==='REGULAR'?'정회원':'게스트'}</span></p>):<p className="muted">아직 배정된 참가자가 없습니다.</p>}</section>)}{unassigned.length>0&&<section className="card team-group"><h3>미배정</h3>{unassigned.map(m=><p key={m.memberId}><b>{m.name}</b><span>{m.memberType==='REGULAR'?'정회원':'게스트'}</span></p>)}</section>}</div>):<div className="empty card"><span>🏀</span><h3>{tabs.find(([key])=>key===tab)[2]}</h3><p className="muted">관련 API가 준비되면 이 화면에서 확인할 수 있어요.</p></div>}</>}</>}
