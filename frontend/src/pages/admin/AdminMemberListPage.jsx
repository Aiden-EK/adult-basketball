import { useEffect, useMemo, useState } from 'react'
import { Link } from 'react-router-dom'
import PageTitle from '../../components/PageTitle'
import { ErrorMessage, Loading } from '../../components/Status'
import { getAdminMembers } from '../../services/memberApi'
export default function AdminMemberListPage() { const [data, setData] = useState(null); const [query, setQuery] = useState(''); const [error, setError] = useState(''); useEffect(() => { getAdminMembers().then(setData).catch(() => setError('정보를 불러오지 못했습니다.')) }, []); const filtered = useMemo(() => (data || []).filter((member) => member.name.includes(query)), [data, query]); return <><PageTitle eyebrow="ADMIN · MEMBERS" title="회원 관리" description="회원 등록, 수정, 상태 관리를 합니다." /><Link className="primary full create-link" to="/admin/members/new">+ 새 회원 등록</Link><input className="search-input" value={query} onChange={(event) => setQuery(event.target.value)} placeholder="이름으로 검색" />{error && <ErrorMessage />}{data === null ? <Loading /> : filtered.length === 0 ? <div className="empty card"><h3>검색 결과가 없습니다.</h3></div> : <div className="stack">{filtered.map((member) => <Link className="league-card card" key={member.id} to={`/admin/members/${member.id}`}><div><h3>{member.name}</h3><p className="muted">{member.memberType === 'REGULAR' ? '정회원' : '게스트'} · <span className={member.isActive ? 'active-text' : 'inactive-text'}>{member.isActive ? '활성' : '비활성'}</span></p></div><span className="round">→</span></Link>)}</div>}</> }
