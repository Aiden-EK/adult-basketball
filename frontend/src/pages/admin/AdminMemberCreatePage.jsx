import { useState } from 'react'
import { useNavigate } from 'react-router-dom'
import PageTitle from '../../components/PageTitle'
import { ErrorMessage } from '../../components/Status'
import { createAdminMember } from '../../services/memberApi'
export default function AdminMemberCreatePage() { const navigate = useNavigate(); const [name, setName] = useState(''); const [memberType, setMemberType] = useState('REGULAR'); const [note, setNote] = useState(''); const [error, setError] = useState(''); const [saving, setSaving] = useState(false); async function submit(event) { event.preventDefault(); setSaving(true); setError(''); try { const member = await createAdminMember({ name, member_type: memberType, note }); navigate(`/admin/members/${member.id}`) } catch (e) { setError(e.status === 400 ? e.message : '회원을 등록하지 못했습니다.') } finally { setSaving(false) } } return <><PageTitle eyebrow="ADMIN · MEMBERS" title="새 회원 등록" description="회원 기본정보를 입력하세요." back /><form className="form card" onSubmit={submit}><label htmlFor="member-name">이름</label><input id="member-name" value={name} onChange={(e) => setName(e.target.value)} required placeholder="예: 김철수" /><label htmlFor="member-type">회원 구분</label><select id="member-type" value={memberType} onChange={(e) => setMemberType(e.target.value)}><option value="REGULAR">정회원</option><option value="GUEST">게스트</option></select><label htmlFor="member-note">비고 (선택)</label><textarea id="member-note" rows="4" value={note} onChange={(e) => setNote(e.target.value)} placeholder="관리자 전용 비고" /><button className="primary full" disabled={saving}>{saving ? '등록 중...' : '회원 등록'}</button>{error && <ErrorMessage text={error} />}</form></> }
